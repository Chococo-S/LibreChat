version: 1.3.3

memory:
  disabled: false
  personalize: true
  tokenLimit: 2000
  messageWindowSize: 5
  agent:
    provider: "openAI"
    model: "gpt-5.4-mini"

modelSpecs:
  enforce: false
  prioritize: true
  list:
    - name: "gpt-5_4-tight"
      label: "GPT-5.4（空行ひかえめ）"
      default: true
      description: "GPT-5.4 with tighter paragraph spacing"
      group: "openAI"
      preset:
        endpoint: "openAI"
        model: "gpt-5.4"
        promptPrefix: |
          改行のたびに空行を入れないでください。
          空行は段落や話題の切れ目にだけ使ってください。
